# 2026-09-18 工作日志

## 更新项目 skill（.trae/skills/playwright-ui-auto/SKILL.md）
用户要求"记住框架技术特点，后续会发页面截图让你继续写用例"，并让先了解项目里的 skill。

- 发现该 skill 有多处**过时/错误**描述，会导致后续生成的代码跑不通，已全量重写：
  - 错写"项目使用 Ant Design"→ 实际是 Vue3 + **Element Plus** + Vben Admin
  - 错写公共方法在 `utils/public_operation.py` → 实际在 **`pages/public_operation.py`**
  - 仍在教 `fill_cell()` / `get_row_cell_by_header()` → 这两个已于 2026-09-17 作为死代码删除
  - 完全没提 **vxe-table**（货物明细表）和 `select_dropdown_option`
- 新版补入内容：
  - 第 0 节 技术栈速查（含"不是 Ant Design"的提醒）
  - 第 3.2 节公共方法：`select_dropdown_option`（首选）/ `repetitive_operation` / vxe-table 五件套
  - 第 4.2 节测试类模板：`@pytest.mark.run(order=N)`、类属性传数据、`input_value()`
  - 第 5 节 新用例标准工作流（用户发截图 → 先写探针 dump 真实 DOM 再写代码）
  - 第 6 节 **15 条踩坑清单**表格（含 FilePath `\u` 语法错误坑）
  - 第 7 节 生成代码 Checklist（含回归基线用例要求）

## 项目记忆补充
- MEMORY.md 新增"协作方式与扩展用例约定"：Trae 工具、skill 路径与"写新用例前必读"、
  不凭截图猜定位的标准流程、`utils/` 不可改。

## 当前状态（供后续接手）
- 基线全绿：`test_create_yikoujia_contract.py` 2 passed（合同 20260917-BDFZ-SFNSS005 创建+审批通过）；
  `test_goods_transfer_in.py`、`test_create_user.py` 回归 2 passed。
- 全局 pyflakes 已清零，临时探针脚本已清理。
